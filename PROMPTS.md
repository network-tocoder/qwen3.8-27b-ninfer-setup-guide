# Prompts

Prompt wording affects MTP acceptance and speed. These are the prompts shown for the application and reasoning tests.

## Compact AI GPU dashboard

```text
Create a compact polished single-file HTML AI GPU dashboard in under 2500 tokens. Include four KPI cards, one animated canvas chart, Start and Reset controls, responsive styling, and working vanilla JavaScript. No comments, no external libraries, no markdown fences. Return complete valid HTML ending with </html>.
```

## AI inference cluster simulator

```text
Build a polished, complex, single-file HTML AI Inference Cluster Simulator under 8000 tokens. Include four simulated RTX 5090 workers, a live request queue, smart routing, throughput and latency charts, adjustable traffic slider, Start/Pause/Reset controls, GPU failure injection, automatic recovery, completed-job counter, event log, and responsive styling. Vanilla JavaScript only, no external libraries or markdown fences. Clearly label all telemetry as SIMULATED. Return complete valid HTML ending with </html>.
```

## Long-context architecture explorer

The request body is generated by `tools/build_codebase_request.py`. Its task instruction is:

```text
Study the supplied NInfer C++, CUDA, headers, and implementation files. Generate a polished interactive, self-contained HTML NInfer architecture explorer. Use only components that can be supported by the supplied source. Include the styling and JavaScript in the same file. No external libraries and no markdown fences. Return only valid HTML ending with </html>.
```

Because the upstream source tree changes, your character and token counts may differ from the recorded 272,773 source characters and 70,268 prompt tokens.

## XHigh concurrency review

```text
Review this asynchronous money-transfer function:

async def transfer(src, dst, amount):
    async with locks[src]:
        if balances[src] < amount:
            raise ValueError("insufficient")
        await asyncio.sleep(0)
        async with locks[dst]:
            balances[src] -= amount
            balances[dst] += amount

Concurrent transfers may run in either direction, tasks may be cancelled, and accounts may be created dynamically. Identify every concurrency and correctness failure, demonstrate a concrete failing interleaving, provide production-safe corrected Python, define its invariants, and write deterministic tests for deadlock, cancellation, negative amounts, and conservation of total balance.
```

